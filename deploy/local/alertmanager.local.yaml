global:
  resolve_timeout: 5m
route:
  receiver: default
  group_by: ['alertname']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
receivers:
  - name: default
    # Local dev: alerts visible in Alertmanager UI at http://localhost:9093
    # Wire Slack/PagerDuty here for production (see deploy/alertmanager/config.yaml).
