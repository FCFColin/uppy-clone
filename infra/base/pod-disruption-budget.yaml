# 企业为何需要：PDB 确保滚动更新/节点维护期间至少保留 1 个可用 Pod，防止全部实例同时被驱逐。
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: balloon-game-pdb
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: balloon-game
